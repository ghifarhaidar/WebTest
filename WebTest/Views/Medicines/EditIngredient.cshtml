@model WebTest.VWModels.MedicineIngredient.EditIngredientInMedicineRequest

@{
    ViewData["Title"] = "Edit Ingredient";
}

<h1>Edit Ingredient</h1>

<form asp-action="EditIngredient" method="post">
    <div class="col-md-6">
        <hr />
        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Ingredient.Name):
            </dt>
            <dd class="col-sm-2">
                @Html.DisplayFor(model => model.Ingredient.Name)
            </dd>
        </dl>
        <div class="form-group">
            <label asp-for="Ingredient.Ratio">Ratio:</label>
            <input asp-for="Ingredient.Ratio" class="form-control" />
            <span asp-validation-for="Ingredient.Ratio" class="text-danger"></span>
            <input type="hidden" value="@Model.Ingredient.Id" asp-for="Ingredient.Id" />
        </div>
        <input type="hidden" value="@Model.MedicineId" asp-for="MedicineId" />
        <input type="hidden" value="@Model.MedicineIngredientId" asp-for="MedicineIngredientId" />
        <div class="form-group mt-4">
            <button type="submit" class="btn btn-primary">Edit Ingredient</button>
            <a asp-action="EditIngredients" asp-route-id="@Model.MedicineId" class="btn btn-secondary ms-2">Back</a>
        </div>
    </div>
</form>


@section Scripts {
    @{
    await Html.RenderPartialAsync("_ValidationScriptsPartial");
}
}
